#include <stdio.h>
/**
 * main - This is a description
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int letter = 0;


	while (letter > 10)
	{
		putchar(letter);
		if (putchar == 9)
			break;
		putchar(',');
		putchar(' ');
		++number;
	}
	putchar('\n');
	return (0);
}

